# FEAT-155 当前授权与调用台账

2026-09-23。用户已明确授权实现剩余需求，要求复用原机制且不扩大范围。历史逐阶段授权和当时停点保存在[归档](history/before-remaining-delivery/04-owner-approval-and-implementation.md)；当前范围见[00](00-feature-brief.md)，本轮执行见[47](47-remaining-delivery-plan.md)。旧阶段的“报告后停止”不覆盖本次完成剩余需求的新授权。

## 已接受决定

- Q-01～Q-04与D0已确认。Desktop native拥有计划与原同库账本，共用worker/Coordinator/outbox；Host薄适配，Runtime保持真实执行/审批/历史权威。
- 用户允许本需求超过16小时；不据此删减其它Must。
- 保存计划保持暂停；有限自动授权与单次许可独立。立即运行/重跑最多一次、最长10分钟，实际任务沿Ask，不代答批准。
- 固定Runtime的最小input-only扩展已由29解决资格阻塞，当前不新增Runtime修改。
- 防自动空闲睡眠、睡眠后停止发送均由用户明确移除，标为OWNER_EXCLUDED。系统通知首期延期；应用内重要更新、正常退出与重开保留。
- 普通SQL15/Store5、隔离候选26/6保持，不迁移日常库。本轮本地提交已获授权；推送、发布、生产操作或跨设备/云调度未授权。
- 仅正常非破坏性验证；强杀、权限破坏、攻击fixture及伪装运行时永不采用。

## 当前调用台账

| 类型 | 累计批准上限 | 已用 | 剩余 | 说明 |
|---|---:|---:|---:|---|
| 真实文本模型HTTP请求 | 14 | 13 | 1 | 起点10；本轮草案、未来自动、普通Ask各1；全部HTTP200。普通Ask只生成文字确认，不冒称原生审批证据。 |
| 图片 | 0 | 0 | 0 | 未调用 |
| 商家接口/外部MCP | 0 | 0 | 0 | 未调用 |

本轮真实请求逐次证据：[计数台账](evidence/remaining-delivery/provider-ledger.json)。失败保守计入；run数不能替代HTTP请求数。采用原有permission-smoke-meter硬上限4，不读取或记录密钥值。余量不意味着可扩需求。

声明式本机Provider单独记账于[local ledger](evidence/remaining-delivery/local-provider-ledger.json)，不联系上游、不使用真实额度，只验证固定Runtime与实际原生页面的组合行为，不能冒称模型质量证据。

## 当前执行与验收

已完成底层基础至4D-1；本轮补齐卡片/记录交互、跨境文案、只读全局重要更新，并按最终候选核查适用Must。当前适用Must及正常重开验证完成；D4结论与未执行项统一记录在02及48，机器声明校验单独记录。

2026-09-23版本固化请求（历史停点）：用户采纳五仓审计→经提交授权后依赖顺序固化→固定版本复验。审计与具体文件/日志/来源更新范围已在49完成；该消息保留提交授权条件，当前无新增Git提交/push/tag/日常库迁移授权。本轮调用0，累计13/14不变。


## 2026-09-24 本地版本固化授权

用户在49具体五仓清单之后明确回复“授权”。授权覆盖审计范围本地提交、依赖来源生成/校验、固定版本必要回归及canonical正常启动/退出/重开；无需再次申请已批准事项。范围内修复复验发现的旧interrupt重复claim缺陷，另立Desktop小提交并保留失败证据，不改变历史、授权或存储格式。

本轮不扩大到push、合并、tag、发布、日常库迁移或新模型调用。实际本轮真实文本0、本机Provider0，累计13/14不变；最终版本与复验见[50关闭报告](50-local-version-freeze-and-closure-report.md)。先前“无提交授权”保留为当时历史，不能覆盖本次明确授权。

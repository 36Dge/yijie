# FEAT-155 五仓交付审计与本地版本固化清单

当前状态更新（2026-09-24）：用户已明确回复“授权”；以下为授权前的审计基线，后续固定版本、发现的最小缺陷及复验以[50](50-local-version-freeze-and-closure-report.md)为准，不再等待提交授权。

2026-09-23。用户采纳三步收尾顺序，其中第2步明确保留“经提交授权后”。本轮已完成第1步及可提交范围准备；尚未执行git add/commit、依赖SHA改写或新版本启动。需明确本地提交授权后，才能形成真实的Runtime→Contracts→Host→Desktop→元仓关闭版本。既有D4结论仍只适用于48记录的源码候选，不预先把未来固定版本写为验证通过。

本轮契约影响：审计/文档为none。后续精确来源固化涉及私有部署/构建锁语义，按semantic审查，前提是wire、数据版本、权限和产品行为全部不变；整体FEAT-155仍为breaking。若发现必须改变业务契约或数据语义，不能混入版本固化。

## 1. 五仓完整范围与无关改动

用最初Q-01证据、当前Git状态、48的最终摘要、全部变更路径和源/生成检查交叉核对：FEAT-155开始时四个产品仓均clean，元仓仅已有该需求目录；五仓HEAD和分支一直未变。未发现需要混入的独立无关修改。共享聊天/退出/权限文件的变化服务于本需求兼容、用途隔离及原执行链复用，不因文件名没有feat155就误排除。

以下为本轮审计起点的提交范围；本轮新增49及审计证据随后补入元仓，不计入起点827。

| 仓库 | 起点文件数 | 本次应固化内容 |
|---|---:|---|
| Runtime | 278 | 6个已跟踪修改、272个新增；input-only最小patch、269份canonical schema、独立构建与说明。固定上游和保留FEAT136产物不改，FEAT137不重启。 |
| Contracts | 76 | 7修改、69新增；计划/执行/草案/恢复/时间、Runtime策略投影、各源/生成物/一致性与兼容检查。旧public/chat/permission wire保持。 |
| Host | 58 | 15修改、43新增；独立只读查询、受限草案/用途及Store6兼容、精确原生资格、同源类型与验证。 |
| Desktop | 161 | 46修改、115新增；SQL16～26及兼容reader、原执行链预约/投递/恢复、typed IPC、页面/草案/授权/历史/通知和检查。普通SQL15保持。 |
| 元仓 | 254 | 1修改、253新增；ADR-0020、完整需求包、分阶段历史、48/D4及证据。 |

逐文件路径、状态、大小和SHA-256见[scope-and-baseline.json](evidence/version-freeze-audit/scope-and-baseline.json)。没有删除原文件。五仓工作分支均为`chore/retirement-baseline-20260905`；不自动改分支/远端。元仓不替代子仓提交。

有意排除：所有已忽略的Runtime二进制/build、Host本地构建和状态、Desktop target/dist/.local、密钥、用户库、node_modules及外部原PDF。只记录可复现源码、已有精确产物摘要和必要证据。对拟提交文本及下列日志进行了私钥/JWT/常见Provider/Git凭据值形状检查，未命中；该检查不冒称完整安全审计。

## 2. 固化前必须处理的具体事项

### F-01：候选基线不是新实现的完整commit

- Runtime投影中`active_base_commit=b2b20e…`是旧有效两补丁来源，不能直接改写其含义来冒充新input-only实现来源。
- Contracts各族`base_commit=db4458…`是生成比较基线，当前consumer快照仍只是候选字节摘要，未记录包含新族的Contracts commit。
- Desktop `scheduled-host-build.candidate.json`仍以旧Host HEAD及工作树摘要校验。Host提交后必须同步更新，否则真实canonical入口会因HEAD不符拒绝启动。

推荐最小修正：保留旧字段原语义，在既有生成/同步及来源校验中固定新Runtime/Contracts完整来源引用；校验Git对象与源/生成摘要一致。Desktop按新Host完整commit及现有封闭文件集合更新来源。所有生成物由同源脚本产生，不手改DTO、伪造基线、不设置任意dirty放行开关。`release:false`保持，无tag、无生产资格。

不建设新的通用发布/依赖框架。已有独立族仍沿既有checker；FEAT152等普通锁只在构建依赖确有需要时做同内容来源固化，不改变权限语义或借本批重做旧功能。

### F-02：关键验收日志当前被Git忽略

根`.gitignore`的`*.log`会使普通git add遗漏48/D4所引用的实际输出。本次明确列出20份主要日志，包含标准门禁阻断、真实启动和账本对照；路径和摘要见scope文件的`explicit_ignored_evidence`。提交时仅显式纳入这些已核对文件；不对整个忽略目录使用强制添加，不更改全局忽略规则、不清理/覆盖历史失败日志。

### F-03：索引状态落后

`docs/adr/README.md`仍写“D4未执行”；本轮已修正为48已通过本地D4、两项电源OWNER_EXCLUDED、系统通知延期、版本固化待收尾。其它历史报告保持当时语义；后续只修正FEAT155当前来源/状态说明，不改写历史验收或扩大为全仓文档翻修。

## 3. 授权后的确切顺序

| 顺序 | 本地提交及依赖 | 完成判断 |
|---|---|---|
| 1 Runtime，记为R | 提交审计清单中的最小input-only源码、脚本及269份schema | 原产物摘要对应R中的相同patch/schema；旧有效产物保留，来源可从Git对象复核。若确需重建，只用canonical独立构建目录。 |
| 2 Contracts，记为C | 根据R补精确原生来源，生成/严格检查；源与生成物同批提交 | 六个新族一致；全部适用四基线兼容检查有结果；不引入自引用伪SHA。 |
| 3 Host，记为H | 经既有同步入口固定C和原生R投影；提交实现/兼容/生成消费 | Store5/6及普通/草案边界保持；安全定向race/vet通过；出站机制不新增。 |
| 4 Desktop，记为D | 固定C/H与原生来源，生成typed IPC/私有锁并校验；提交完整需求实现 | 真正Git对象和当前文件相符；普通15/5、候选26/6保持；旧Contracts dirty阻断按实际清除情况复核，不绕过。 |
| 5 固定版本复验 | 同源、必要定向回归、canonical候选正常启动/退出/重开；复验原有记录和关联 | 不迁移日常库、不自动触发新模型或业务操作；来源变化若导致行为变化，先修复并重新验证，不继承不适用证据。 |
| 6 元仓，记为M | 记录R/C/H/D、产物/源摘要、复验、残余限制和关闭结论，提交元仓 | 最后M由Git输出定位，不在提交内填写自身未来SHA。未推送/发布与本地已固化区分。 |

按依赖单仓完成，不用一次`git add .`跨仓扫描。R/C/H/D需实际产生后才能写下游；必要的纯来源/检查脚本修改纳入对应仓审查。预计每仓一个主提交，只有明确的来源固定依赖要求时才拆最小跟进提交，不为追求固定提交数做循环pin。

## 4. 本轮已完成的提交前检查

- 四个产品仓与D4源码摘要相同；各检查后已有文件逐字不变，五仓HEAD不变，暂存区均空。见[post-check-state.json](evidence/version-freeze-audit/post-check-state.json)。这是审计准备，不是固定版本验收。
- Contracts六族源/确定性生成/严格校验通过；7个定向文件29项PASS，0 skip，见[contracts-tests.txt](evidence/version-freeze-audit/contracts-tests.txt)。
- Host `internal/app`、`internal/session`的`^TestFEAT155`及Codex两项安全配置检查均race PASS，见[host-tests.txt](evidence/version-freeze-audit/host-tests.txt)。本轮未运行原生集成/模型，未使用禁止故障套件。
- Runtime shell/Python语法PASS；Desktop私有IPC同源检查PASS。未重建或覆盖现有Runtime二进制。
- 本轮付费0、本机Provider0、图片/商家/MCP0。额度仍13/14余1，不因固化版本就重复消耗。

## 5. 当前停点与批准对象

第1步审计完成，第2/3步尚未执行。待批准对象是：以上五仓完整FEAT155文件清单、20份主要日志及本轮交付记录；允许必要的精确依赖来源/生成/校验修订、按上述顺序进行**本地Git提交**并完成固定版本零模型复验。

此批准不包含push、合并、tag、发布、普通入口启用、日常库迁移、扩大功能、新增模型额度或重做两个已排除的电源子项。

之所以保持条件停点：最新用户消息保留“经提交授权后”，且元仓AGENTS.md第88行要求“不自动commit、push、创建分支或改远端，除非用户明确要求”。前序需求实现授权明确排除了Git提交，不能以D4已完成替代提交授权。本轮先交付可审查的具体清单，再请求一次明确授权；不重复请求已批准的实现或验收权限。

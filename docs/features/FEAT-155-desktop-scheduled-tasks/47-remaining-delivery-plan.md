# FEAT-155 剩余需求实施与范围收口

> 已按本范围完成，最终交付与适用验收见[48报告](48-remaining-delivery-report.md)。本文保留执行前定义，当前调用台账以04和48为准。

2026-09-23。用户明确授权实现46审计后的剩余需求，并明确防自动空闲睡眠不实现。该项记为OWNER_EXCLUDED，不作为首期阻断、不伪造PASS。系统通知保持此前延期；用户随后进一步明确移除“睡眠后停止发送”，该子项同样OWNER_EXCLUDED，不为其新增实现、测试或作为D4阻断。正常退出管理保留。本轮直接推进实现和适用验收，无需再等待阶段实施批准。

## 当前范围

1. 卡片历史/立即执行/删除三图标及hover、focus、tooltip；启停开关沿原有限确认，不点击即授无限权。卡片主体编辑、记录主体详情，键盘等效且子操作不冒泡。
2. 跨境占位词、指定状态文案、按plan ID定位历史、记录深链和弹层焦点。复用原查询、表单、单次/自动许可及删除事务。
3. 应用内重要更新复用NNotificationProvider及已有持久run/needs_attention事实。新增最小Desktop私有只读查询：当前scope最近运行及需处理项，最多50条，需处理优先；只返回run/plan身份、快照名称和有限状态，不返回提示词、不启动Host、不修改时钟/预约/额度。源schema先于Rust/TS/command生成与消费，当前变更additive，整体FEAT仍breaking。
4. 全局观察器同一时刻最多一个读请求，每3秒刷新；失败退避，不依赖打开某个聊天。首次快照只提示未处理事项，旧结束记录不刷屏；当前会话内按run和状态去重，恢复后未处理项可再提示，结束历史一直保留在执行记录。通知点击进入该run的原生详情，再由既有逻辑核对聊天关联。撤权/切scope销毁提示，查询及提示不产生执行资格。此为最小应用内提示，不建设通知中心/新状态机/新持久表。
5. 当前普通SQL15/Store5、候选26/6保持。无新SQL迁移，无Runtime/Host/共享Contracts业务变更，无日常库迁移。若核查发现必须改变这些边界，先记具体原因，不能靠开关绕过资格。
6. 先完成同源/安全定向测试，再用canonical隔离候选完成剩余UI与适用Must整体检查。真实文本使用既有余4次额度，最多4次，先验证最小场景再消耗；图片、商家/MCP外部0。所有失败照常计费，不清零旧账。使用纯文本/普通合成场景，不强杀、权限破坏、攻击fixture或替换Runtime。

## 验收口径

原AC-001至009保留。AC-010去掉防自动空闲睡眠开关及其释放验收，同时去掉“睡眠后停止发送”要求及专项验收；仍要求真实复用、权限、预算、去重、正常退出/重开、应用内重要更新、亮暗/最小窗口/键盘。原生审批沿既有机制，不新增审批器，不需要商家接口验收。已有失败历史、定向合成和当前真实验证分别标明来源，不把不同批次拼成一次fresh证据。

完整D4必须由当前候选的适用Must与代表性正常失败/恢复证据支持。用户禁止的异常注入永不执行；全量dirty门禁和既有非本需求失败如实登记，demo_fast不扩大成全仓full-green专项。没有Git提交/推送/发布/生产授权。

完成后形成48交付报告、当前验收矩阵和证据索引；若外部状态导致某项无法验证，明确具体缺口，不能把“代码完成”当D4完成。

用户范围补充：“睡眠后停止发送”明确移除、不实现。既有公共生命周期代码是前序事实，本轮不为删除它扩成普通聊天/Host生命周期重构；不新增该特性、不将其算作当前交付要求。设备休眠/退出后不承诺持续执行，范围没有扩为常驻运行。

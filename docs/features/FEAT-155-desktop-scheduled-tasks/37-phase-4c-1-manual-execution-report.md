# FEAT-155 · 4C-1 有限授权、手动运行与结果定位实施报告

2026-09-22。按用户授权完成[36方案](36-phase-4c-1-manual-execution-plan.md)的本批范围，**报告后停止**。暂停计划现在可以经过一次明确确认，沿原 worker、Coordinator、Host 和固定 Runtime 手动运行，并从原 run 打开精确的聊天轮次。没有进入自动触发、独立重跑、唤醒、通知或完整 D4。

## 实现与边界

| 项目 | 本批结果 |
|---|---|
| 原请求回执 | 私有同源 IPC 新增 `schedule_read_execution_receipt_v1`，按 scope、grant/manual 与原 request ID 查询已有表；不依赖管理写回执，不创建许可。Rust/TS、strict validator 与命令注册一致。 |
| 仅手动准入 | 实际候选装配 ManualOnly；写入口拒绝 enable/rerun，claim 与实际 POST 重验当前许可。普通入口不安装发送权威，候选不安装自动触发资格或 trigger lifecycle，保留原恢复/观察 tick。 |
| 有限许可 | 原生要求暂停计划、`max_runs=1`、截止不超过确认时刻加600秒。行动许可另绑定当前 UI context/revision、Host nonce、生命周期 epoch、grant/run 与原 create→turn 操作；当前 UI 授权可能更早到期。原 run 的最后一次已占额度可以完成投递，不能再次扣额。 |
| 失效和恢复 | 从未尝试的操作原子取消、一次退款、释放预约及聊天索引占用；尝试过的操作不退款、不重投。映射缺失也只凭精确的正常停止证据释放 unknown，保留历史。读回执、权限刷新及重开均不补建行动许可。 |
| 页面 | 管理读取保持不启动 Host；点击立即运行显式准备原执行链，审阅当前版本、内容、目标和 Ask 后提交两个既有事务。grant/manual 分别持稳定请求 ID；回执不明停止后续步骤，查询不自动发送，未提交的原 manual 只能显式继续。 |
| 结果 | 受理与完成分开，原生结果与业务结果分开；记录保留历史快照，执行时间和耗时仍诚实显示未知。原生再次核对 conversation/local_turn 关联，ChatPage 经既有授权历史分页定位，最多连续加载10页，找不到不会跳最新轮冒充。 |

`contract-impact=breaking` 保守审查：存量候选出站资格收紧；新增私有查询本身 additive。没有新增公共 Host API、Runtime 方法、产品依赖或 SQL migration。**普通 SQL15/Store5、候选 SQL23/Store6保持**。Contracts、Runtime 源码与固定二进制保持；Host 仅新增普通 Ask 的固定产物验证，并给既有计数器补安全的传输异常类别，不记录请求正文或凭据。候选 Host 来源锁只增加经过审查的新测试文件，先逐项确认所有原来源摘要未变。

## 验证与真实调用

[检查索引](evidence/phase-4c-1/checks.json)、[实际 UI 记录](evidence/phase-4c-1/native-ui.json)、[最终只读账本](evidence/phase-4c-1/ledger-final-read-only.log)、[范围审查](evidence/phase-4c-1/workspace-review.json)。本任务完成源码及反向场景审查，没有冒称独立人工或代理批准。

| 证据 | 结论 |
|---|---|
| 原生 | 111项 FEAT-155 阶段回归通过；后续新增映射缺失停止证据检查，以及最后的目标标题收尾后14项相关检查通过。新6项4C-1检查涵盖三目标组合、重复、旧许可、scope、发送前撤权、一次退款、未知释放。后两组与111项有重叠，不相加冒称总数。 |
| 私有契约 | 43个原生交换、26个命令通过原生/strict AJV/TS一致性；同源生成检查通过。 |
| 前端 | 最后43项定向检查通过；ChatPage 46项通过，原有3项显示断言继续失败，与33/35基线一致。新增精确轮次定位检查通过。类型、定向 lint、Rust fmt/clippy、Host scoped vet 与 canonical 构建通过。 |
| 普通 Ask 资格 | 真实固定 Runtime＋真实 Host adapter＋声明式本机 Provider完成普通 start/turn、正常退出、resume/turn；2次本机请求、0次付费，实际策略为 `on-request / user / workspaceWrite / networkAccess=false`，共享计数路由及两种重试均为0。没有将草案 input-only 资格当作普通任务资格。 |
| 真实 UI | 一次确认、取消不提交、原生终态、记录详情、精确第1/2/3轮定位、正常重开、亮暗主题、1180×760最小窗口及焦点恢复均已核对。原浅色外观已恢复。 |

本批严格使用[独立计数台账](evidence/phase-4c-1/provider-ledger.json)，起始累计4次，初始本批上限3次。用户随后明确额外授权2次并让本任务判断必要性：仅使用其中1次补齐第三类目标，计数器从3提高至4、批准上限5，另一份追加额度保留。旧3次请求完整封存，没有清零历史或直连绕过计数。

| 本批请求 | 目标与结果 |
|---|---|
| 1 | 专属聊天首次运行：传输结束，未取得 HTTP 状态，原生 failed；保守计费1次。未自动重试。之后无模型 TLS/GET 检查正常；旧计数器未保留异常类别，不能追溯断言该次具体原因。 |
| 2 | 用户流程内新的手动确认，复用同一专属聊天：HTTP200、原生 completed；第2轮可读“4C1 专属聊天验证通过”。 |
| 3 | 已有聊天目标，选择本批普通合成聊天：HTTP200、原生 completed；同一聊天第3轮可读“4C1 已有聊天验证通过”。 |
| 4（追加1次） | 每次新建聊天：HTTP200、原生 completed；创建不同的普通会话，在新会话第1轮可读“4C1 新建聊天验证通过”。 |

当前累计 **8/12，剩余4次**；图片、商家接口和 MCP 外部调用均0。追加授权2次中只用了1次，另一份无需使用。三种运行目标均已有真实成功闭环；“每次新建聊天”的多次重复分配仍由零调用组合证据覆盖，没有为了重复验证再消耗额度。

只读 SQLCipher 核对：4个 manual run、4个一次 grant、6条相关 outbox，每条 attempt=1；3个 completed、1个 failed，全部依原生终态释放，预约数0，均未退款。三个本批计划保持 paused。前三次运行正常退出重开后行、绑定、额度和操作计数不变；补充的第四次完成后正常退出并从只读库复核。旧4B草案/计划可读。实际验收只使用隔离候选库，日常库未迁移。

## 保留的限制与收尾

- 真实 Provider 消息缺失可用 phase，原聊天 UI 保留“未分类模型消息”、partial 与历史差异提示；展开可读真实文本，没有伪造 `final_answer` 或完整历史。三项既有 ChatPage 断言不在本批扩成显示重构。
- 本批未制造真实审批、异常进程退出或其他故障。审批、忙碌和失效边界由定向组合证据覆盖；实际退出均走正常流程，没有强杀、权限破坏、攻击资源或二进制替换。工具在退出后查询窗口曾重新唤起无环境空 UI，已单独正常关闭，未发送或迁移数据。
- 初次回归遇到证据目录未预建、前端命令误包含旧构建副本；修正验证命令后通过。Rust lint 的同类型转换警告已修正。没有将这些失败输出当作通过证据。
- 回退仍先关闭候选并正常退出，再使用兼容23/6的只读禁发 reader；不降级写库或覆盖数据库。未提交、推送、合并、部署或发布。

4C-1 已收口。完整 FEAT-155 仍 active；自动启用/触发、独立重跑、完整历史时钟、电源与应用内重要更新，以及全部 Must 的整体 fresh run 和 D4，仍按后续批次推进。**本轮到此停止。**

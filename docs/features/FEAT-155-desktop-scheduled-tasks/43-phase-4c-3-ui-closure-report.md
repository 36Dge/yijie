# FEAT-155 · 4C-3 视觉与交互收口报告

2026-09-23。按用户明确授权执行[42方案](42-phase-4c-3-ui-closure-plan.md)，**4C-3视觉与交互收口完成，报告后停止**。普通SQL15/Store5、隔离候选25/6保持；没有新增存储版本、迁移日常库或推进历史时钟、电源和通知。本批真实调用0，累计仍10/12、剩余2次。

## 调查结论与最小修正

首先使用未经本批修改的源码，通过canonical入口重建并打开同一隔离候选。与41报告记录的40个Desktop变更文件逐一比对，摘要全部一致。此次原生截图与AX树均能显示确认弹层，暗色下shell、卡片、弹层与文字同步更新；默认取消、Tab/Shift+Tab和关闭返回可观察。

因此，41中的“截图不显示弹层/暗色不完整”在本轮相同产品源码下**未复现**。历史发生原因仍未知，不能据此断言一定是工具或某个WebKit问题。本轮用新的真实原生画面和操作证据关闭当前可见性验收缺口，没有为猜测中的主题故障修改全局主题、CSP或Runtime。

实际复核发现并修正两处有证据的问题：

| 问题 | 原因与修正 |
|---|---|
| 长差异时默认取消焦点不可见 | 最小窗口中整个Card共同滚动，按钮落在可视区外。单次运行/重跑确认改用现有NCard footer承载许可说明与操作，正文独立滚动，标题和操作保持可见。正文增加有名称、可聚焦的滚动区域及现有token焦点线，支持PageUp/PageDown。 |
| 详情切换重跑时焦点落到正文 | 两个Modal离场/入场重叠，在正文可聚焦后复现焦点被交接到首个正文节点。先关闭详情，在after-leave后调用原只读重跑预览；context/授权变化及允许离页时清理尚未交接的UI意图。最终默认焦点稳定在取消。 |

产品只修改Desktop的`src/pages/schedules/ScheduledTasksPage.vue`。复用原Naive/Yj组件、token和确认handler；manual/automatic hooks、原生许可、IPC、SQL、Host、Runtime、额度、恢复与实际发送代码均未修改。`contract-impact=none`，仅纯UI显示与弹层焦点交接；FEAT-155整体及40/41的breaking分类保留。

## 原生验收与检查

[实际观察记录](evidence/phase-4c-3-ui-closure/native-ui.json)、[初次取证](evidence/phase-4c-3-ui-closure/initial-observation.json)、[检查索引](evidence/phase-4c-3-ui-closure/checks.json)、[源码与范围审查](evidence/phase-4c-3-ui-closure/workspace-review.json)。原生截图及AX结果在本任务CUA工具记录中，JSON是观察索引，不冒称另存的图片或DOM计算样式证据。本次无需用户代验，也未使用浏览器mock替代原生结果。

| 项目 | 本轮结果 |
|---|---|
| 立即运行 | 已有暂停计划的当前配置、Ask、一次/最长10分钟及自动额度独立说明可见；默认取消。开启/完成计划的执行语义沿用41及既有定向检查，本轮未制造新运行重验。 |
| 两类重跑入口 | 列表与详情均能进入确认；原/现名称和内容差异准确。无差异记录明确“仍会创建新运行”。未执行项继续禁用重跑。 |
| 亮暗与最小窗口 | 在配置的1180×760最小外框核对长差异；正文滚动，许可及按钮不被挤走。系统亮暗切换保持review与焦点，不丢当前配置；共享聊天页暗色只读结果也可读。 |
| 键盘 | 默认取消；Tab循环为取消→确认→正文→取消，Shift+Tab可反向进入正文；PageDown/PageUp只滚动正文，焦点清晰。Enter取消与Escape返回原立即运行/对应记录入口，详情交接后没有双弹层焦点争用。 |
| 结果定位 | 从已有4C-3记录打开`/chat/01a0cc09-ad6a-7041-bdf5-0e36b511c29c?turn=01a0cc09-ad6a-7041-bdf5-0e5611018757`，定位第1轮，展开可读“4C3 独立重跑验证通过”。这是41的既有结果，非本批新请求。缺phase仍如实保留。 |
| 正常重开 | 正常退出、canonical重开，计划保持暂停，确认框和取消焦点再次正常；随后应用及本机Provider均正常退出，原系统浅色外观已恢复。 |
| 定向与构建 | 最终11文件76项前端检查通过；lint/类型检查、私有IPC生成漂移检查、canonical packaged构建通过。构建保留既有chunk大小提示，不扩展成打包优化。 |

本轮仅进行打开、审阅、滚动、取消和读取历史，没有提交新的运行。只读SQLCipher工具限定隔离候选，使用read-only/query_only；[最终账本](evidence/phase-4c-3-ui-closure/ledger-final-read-only.log)与本批初始及退出重开前输出完全一致，grant、run、outbox、自动额度、原历史和预约均未变化，预约仍0。所有验收计划保持暂停或已删除状态。

使用无上游转发实现的[本机Provider](evidence/phase-4c-3-ui-closure/local-provider.py)作为验证边界，[本机台账](evidence/phase-4c-3-ui-closure/local-provider-ledger.json)请求数也是0。**本批真实文本0、图片0、商家/MCP外部0；累计10/12，剩余2次。** 无需重新消耗41已成功的真实重跑额度。

标准`pnpm test`本轮再次遇到既有Contracts tracked changes保护，在生成检查阶段阻断，没有绕过、清除用户改动或报告全量通过。本批没有Rust/native行为变化，未重复124项原生回归或全量Rust；41的相关证据保持历史来源，长期禁止的可执行文件/权限故障fixture仍未运行。没有新增与样式实现互相复制的测试，修正由现有定向检查及实际原生前后对照验证。

## 审查与停止

实现后另行核对变更与范围：只有一个产品文件变化；Host、Contracts、Runtime与本轮起点逐文件一致。详情交接只打开只读预览，取消不授grant、不发送、不续额；现有原请求查证、权限撤销、unknown和已完成计划语义没有放宽。此为本任务技术自审，不冒称独立人工批准。

UI-4C3-01当前验收缺口由本轮原生证据关闭；另行复现的长内容/焦点问题已修复并验证。41中的原执行证据与43中的UI收口证据分别保留，不将不同批次拼写成一次完整FEAT-155 fresh run。**4C-3收口，完整Must及D4仍未完成；本轮到此停止。** 未提交、推送、发布、强杀进程或修改日常库。

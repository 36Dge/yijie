# FEAT-144 验收场景设计（尚未执行）

2026-09-10。全部为未来设计；本轮不创建fixture、不启动应用、不调用模型。F144场景不替代GS-004真实MCP D4。

## 数据、入口与建议预算

以后获得实施及真实调用授权后，在新建普通验证项目放一张简单色块/文字PNG：images/product.png；images/not-present.png从未创建。不使用商家/账户/凭据信息，不构造损坏图片或攻击载荷。可以用标准静态绘图工具制作，不用图片生成模型。

成功请求：“请使用view_image查看./images/product.png，说明主要颜色和文字。不修改文件，不调用图片生成，不执行其它工具。”工具由Codex决定是否调用；无真实ImageView即不算成功，不用附件预览/助手口述/假事件替代。

失败请求：“请用当前原生命令工具执行test -f ./images/not-present.png，仅检查这个路径是否为普通文件；不要创建、修改、搜索其它文件或重试。”观察既有Command的真实status与exitCode；预期检查不通过。恢复时用户明确请求查看有效图片。Command失败不是ImageView失败，exit1不能推导唯一原因。

尽量用同一项目/会话完成成功、Command失败、恢复，减少标题请求。正常重开不得新增模型请求。建议独立上限10次MiniMax-M3 Responses（含图片输入、标题、审查、自动请求/重试）、0生图；当前未授权、额度0，不借旧额度，达到未来批准上限停止。

## 场景与可判定结果

| 场景 | 输入/操作 | 证据与限制 | AC |
|---|---|---|---|
| F144-S01真实看图 | 显式请求view_image读取普通PNG | 原生ImageView身份与实际通知，卡片安全标签/观察阶段，助手回答独立；未调用则NOT OBSERVED且不关闭AC | 001/002/003 |
| F144-S02正常失败与恢复 | 显式test -f从未创建路径，再新意图看有效图 | Command原生status/exit/安全输出，预期普通文件检查失败；不假造failed ImageView、不自动创建/重试；恢复出现真ImageView | 004/007 |
| F144-S03自身失败可见性 | 源码与普通缺字段/无ImageView的定向测试；真实仅自然遇到时记录 | view_image读取失败先于Item创建；无failed/error不造事实；助手解释不作为原生失败。无需追加模型请求制造该样本 | 003/004 |
| F144-S04状态与隔离 | 普通合成Turn结束、缺记录、未知、容量、并发会话测试；真实会话切换 | Turn不封口Item；历史/断线不忙碌；不串ID。原生中断如未自然遇到记NOT OBSERVED，不故障注入 | 005/007 |
| F144-S05正常重开 | S01保存后正常退出重开同一任务 | 同一原生ID/来源/安全标签/观察方法；已观察事实不被缺失冷历史覆盖；旧历史可访问 | 006 |
| F144-S06安全与权限 | 安全标签、隐藏、容量、空字段普通测试＋权限入口检查 | 无绝对路径/URI/base64/raw error；不为卡片读文件；只复制安全文字；保持请求批准，不扩大访问 | 003/007 |
| F144-S07UI回归 | 适配到真实卡片测试及light/dark、1180×760、200%、键盘/折叠/复制 | 观察与回答可区分，无永久忙碌/遮挡；Composer、Command、附件、Artifact、FEAT-152无定向回归 | 008 |
| F144-S08来源与兼容 | 契约/生成/来源、旧新消费者与保存读取方向、安全自审 | 不原地扩旧v7 enum；唯一缓冲/保存；无假MCP、推演/对账/新执行器；Runtime不变 | 009/010 |

## 证据边界与停止条件

ImageView completed只证明原生查看记录结束，不保证解码/模型理解完成。started/completed可紧邻到达，不要求人为拉长中间状态。无Item不能当failed；模型文字仅为回答；只有原生事实能决定执行记录。Command辅助失败不得填成ImageView或MCP失败。

已观察SQLCipher与冷thread/read分别验收，冷ImageView可能缺失；不读rollout、复制builder或启用实验历史。既有MCP真实注册、进度、失败和业务结果仍延期未交付。旧测试数和D4不继承。

只有新的实施指令后编码；真实调用另核授权。使用日常canonical pnpm tauri:demo-fast:app，普通app身份/现有app-data和Host Home；切换应用前让活跃Turn正常结束并正常退出。来源失败、无法正常退出、版本/数据兼容无法确认或额度不足时停止对应步骤。无攻击/故障fixture、强杀、权限破坏、二进制替换、DB/凭据复制。

实现后分离阶段审查，执行安全定向测试、生成/来源与文档门禁，再做真实canonical。十项Must全部满足且S01/S02/S05真实用户结果完成后才D4 PASS；不支持的ImageView failed/progress/duration记“不适用：原生无该字段/事件”，不是PASS，也不降低旧MCP AC。

最终报告包含：范围与MCP延期、复用入口、源版本/真实pin、删除与兼容清单、测试和canonical数据范围、调用累计、并发保护、未观察/未执行项和限制。提交/推送/部署仍需新的适用授权。

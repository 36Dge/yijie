# FEAT-144 用户范围决策与业务预算

2026-09-10。本轮仅更新需求/台账、审查调整后的D0，不修改产品代码、Runtime、migration、来源锁或检查器。实施仍须遵守此前“D0通过并获实施授权后”的边界。本次明确授权是范围调整及10次业务调用预算，不能转作模型、提交或推送授权。

## 用户决策与适用范围

用户明确指示：“不做业务失败场景的验证，跳过这个逻辑实现”；并确认“业务激活余额充足，可以放心的真实调用，先授权10次”。这项当前明确指令取代旧包中AC-004/F144-S03的强制业务失败实现/验收要求。

- AC-004从活动Must列表移出，原编号不复用，标为OWNER_EXCLUDED / NOT RUN，不能记PASS。原四文件及场景已逐字保存在[范围调整前快照](history/2026-09-10-before-ac004-exclusion/README.md)，原生连接失败记录和此前D0 BLOCKED结果仍保留。
- 不新增Sorftime业务错误分类、专用失败页面/分支、错误字段扩展、失败样本或失败触发流程；不再向官方索取该场景，也不花调用预算制造/寻找失败。
- Codex已具备的原生执行结果及已有通用错误/安全处理照常复用。偶发failed不改成completed，不吞错；审批拒绝、来源检查、秘密保护、容量与兼容检查仍有效。这不属于重新实现被排除的业务失败流程。
- 当前活动Must为AC-001/002/003/005/006/007/008/009/010共9项，全部仍pending；产品实现/canonical/D4均NOT RUN。D0只确认明确后的实施范围，不能继承旧D4或把排除算通过。

## 10次业务授权如何计数

[业务台账](evidence/sorftime-business-call-ledger-2026-09-10.json)与[原生元数据台账](evidence/sorftime-discovery-ledger-2026-09-10.json)分开：

| 类别 | 上限 | 已用 | 口径 |
|---|---:|---:|---|
| Sorftime业务 | 10 | 0 | product_detail的tools/call尝试，包含原生重发 |
| 原生元数据 | 10 | 8 | 原生元数据操作，含正常握手/重试/清理；旧口径不变 |
| 模型 | 0 | 0 | 尚无本需求模型请求授权 |
| 图片 | 0 | 0 | 尚无本需求图片请求授权 |

每次业务请求只查询一个公开ASIN并显式amz_site=US，原生enabled_tools只允许product_detail。公开候选B07H9PZDQW仍不是已知有Sorftime数据的保证，不自动扩为批量或其它工具。

将“10次”按保守上限执行，不能解释为10次逻辑调用外加免费重试：固定Runtime一次逻辑调用可能在session404后原生重发一次；启动前保守预留2次业务尝试，不足则不启动。计数不能可靠区分时保留已预留的上界，不自动返还；不另建MCP重试器或修改Runtime。元数据不是业务，不能互相借额度；必要的新初始化仍计入剩余2次元数据操作。

余额充足是用户确认，未查询私有账户；接受本次明确预算，不再因未知具体扣费公式阻塞。仍不声称官方保证幂等、重试免费或每次只扣一次。已发送请求即使失败/拒绝后也照实计账。没有实现、模型或外部发布的隐含授权。

[14备用手工取样](14-bounded-native-business-sample-2026-09-10.md)若以后确有必要，使用这份业务额度，不再重复索取同范围预算；其原生RPC不经过模型预审批、没有McpToolCall Item，不能代替产品/D4。当前不是D0必要步骤，不为消耗额度而执行。

## D0与后续D4的准确含义

此前D0唯一剩余规划缺口是强制的业务失败场景。用户现已明确将其移出范围，费用也已由用户确认和预算覆盖，因此重审产品/UX、原生机制、9项活动AC、权限与兼容设计后，可重新运行D0。实际检查结果记录在[本轮检查](evidence/owner-scope-d0-checks-2026-09-10.json)，不能仅修改状态宣称通过。

通用D4检查器仍保留representative_failure字段；不修改全局门禁，不将业务失败排除写成该字段PASS。该字段后续只记录AC-007仍要求的既有输入/原生权限边界正常拒绝回归，例如用户在原生Prompt选择decline且工具不调用；它不是Sorftime业务失败，也不恢复AC-004，不新建业务失败逻辑。真实成功、9项活动AC、兼容/来源及相关回归仍需实际验证。

下一步按Contracts→兼容reader→Host/Desktop原生适配→定向验证→canonical→真实D4推进，受影响才更新真实来源。当前未启动产品实施；模型调用仍需独立预算，提交/推送仍按本次明确授权办理。FEAT-152语义、FEAT-137退役、固定Runtime与所有并发修改继续保护。

## 本轮实际结论

需求与设计按上述明确用户决策更新后，strict及D0均实跑exit0/PASS；元仓lint、50/50测试、19个已提交需求包审计（保留5个旧schema v1警告）、Shell及diff检查通过。当前9项Must仍pending，AC-004为用户排除，产品实现/canonical/D4未运行。业务0/10、元数据8/10、模型/图片0，本轮无新增调用。

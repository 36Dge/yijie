> 当前状态：用户于 2026-09-07T06:39:38.090Z 已批准本方案并追加10次；该真实接管方案已执行通过。原始提案和当时预算建议保留下文，不能当作当前待批准或额度。实际结果见 [最终真实流程](../final/real-flow.json)；当前累计35/35，D4剩余项见 [最终补验](../final/remaining-d4-check.md)。

# 人工接管分支：待确认的真实验收方案

用户已授权构思正常操作和允许范围；没有现成自然拒绝任务。本方案与新增调用额度尚待确认，当前未发起付费任务。

## 已完成的前置工作

- Clippy 的复杂 tuple 已命名，未改 SQL、字段顺序或权限语义；严格 Clippy、fmt 与 Native 3 项相关测试通过。
- 实际 retained Runtime 接受 `auto_review.policy` 的原生配置读写和 thread/start；这个前置检查没有 turn/start、模型请求、审批通知或结果。
- Host 的候选配置输入 `YIJIE_PERMISSION_VERIFICATION_POLICY_FILE` 仅在现有 exact local/demo_fast 权限 gate 与固定计数器同时启用时生效。缺省不传策略，原流程不变；文件限绝对路径、普通文件、有效 UTF-8 和 64 KiB。

## 场景与允许范围

在独立 workspace 发起一个普通文件写入任务：只创建 `yijie-desktop/.local/feat152-s4/manual-review/pending/reviewed.txt`，内容 `S4-manual-approved`。目标目前不存在，不读凭据、不联网、不操作已有用户文件、不运行其它任务。

该目录作为待人工复核区域。通过官方 `auto_review.policy` 配置增加一条局部要求：对这个精确文件的首次写入，需要原生自动审核拒绝并请求人工复核；用户通过原生接管明确批准后，复核要求才满足。原生默认 `policy.md` 原文完整保留，只追加此规则，不伪造危险等级，不放宽其它规则。完整原文和校验值见相邻 policy 文件及 plan JSON。

这是**指定原生规则下的真实审核与接管验收**，不是默认策略自然触发风险拒绝。若用户不接受这一配置验收范围，AC-003 继续保持未验收；即使接受，实际模型若返回 allow 或其它结果也如实记录，不能注入或改写回调。

## 操作与判定

1. 通过 canonical 完整应用，在新项目/任务选择“帮我批准”，发出上述普通写入请求。
2. 检查真实 guardian 结果、父/子请求账本和文件未执行状态。只有出现实际 denied 才继续接管验证。
3. 审阅具体卡片的操作与范围后，点击“批准本次”，确认 Host 调用原生 `thread/approveGuardianDeniedAction`，准确关联 review/turn/action；该步骤自身不产生模型回合，不能宣称文件已执行。
4. 原回合结束后在同任务继续已批准操作；维持 auto 模式，让原生处理批准上下文。核对最终执行与文件内容，不能跳到 full 或再批准其它动作来冒充接管成功。
5. 同一次决策重复提交不得再次注入操作；真实卡片不再提供未决批准，协议幂等测试作为补充。
6. 正常退出并移除验证环境变量，保留原始证据与失败。配置文件仅为本地验证资料，不写入生产策略。

## 需要人工介入的时点

- **现在**：确认是否接受这条指定规则的真实接管验收方式，以及追加调用额度。当前 23/25、剩余 2；预计初次父请求/审核/回复 3 次，接管后父请求/再次审核/回复 3 次，审核若各需一次查验工具则共 8 次。建议追加 6 次，累计上限 31，届时可用 8 次；实际仍按账本硬上限计数。
- 如 Mac 锁屏：仅需手动解锁后回复“已解锁”，不需要修改数据库或点假拒绝。
- 如出现超出上述精确普通文件范围的操作：暂停该动作，反馈具体操作与范围；不通过新授权规避用户禁止的攻击、破坏或运行时替换。
- 如无实际拒绝或预算不足：保留未验收项，汇报原生结果及剩余预算，不伪造成功。

真实普通文件审批和已授权测试的 UI 操作可由本任务执行；不需要用户手动制造拒绝事件。

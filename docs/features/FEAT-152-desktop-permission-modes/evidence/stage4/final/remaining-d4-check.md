> 已完成：用户于2026-09-07T07:19:55.228Z追加10次；本清单第36–37次真实请求已通过，并完成正常重启与任务隔离。当前37/45，剩余8，无后续付费步骤。以下保留执行前的具体补验清单与当时预算状态。

# FEAT-152 最终 D4 补验清单

当前实现和单项 AC 通过，真实接管缺口已关闭。仅缺最终 Native 转发修复后构建的请求批准实际回归；不新增产品功能、不改核心、不重复探索拒绝场景。

## 精确下一步

1. 使用同一最终源码和 canonical `pnpm tauri:demo-fast:stable` 启动，保留现有任务/历史、固定 Runtime、普通默认原生审核规则和同一个计数账本；不再启用临时文档复核策略。
2. 打开已完成任务 `01a07aa5-6f91-7133-82c6-0cdd9e9bace4`，确认恢复“请求批准”。同一最终验收任务此前实际完成 auto deny→人工批准→auto续跑→full→ask。
3. 一轮发送三个清楚的正常动作：在项目内新建 `workspace/final-ask-inside.txt`，内容 `S4-final-ask-inside`；向 `https://example.com/` 发一次 HEAD 请求；尝试向项目外 `pending/final-ask-rejected.txt` 写入 `S4-final-ask-rejected`。目录均位于本需求 `.local/feat152-s4/manual-review/`。
4. 观察项目内写入无需审批；对原生联网卡片批准本次，对项目外写入卡片拒绝。批准/拒绝由真实 UI 发出，禁止伪造请求/回调。核对 Runtime on-request/user/workspace-write/network=false、HEAD 正常结果、被拒文件不存在、请求关联独立、忙时禁用及回合结束。
5. 正常退出/重启再读回状态与历史，复核未受影响的 UI/Native/契约证据来源，更新最终验收矩阵并运行 strict D0/D4。若 D4 仍有真实不足如实保留，不能只改 YAML 取得绿色。

## 付费边界

当前授权上限35、已用35、剩余0。本清单尚未执行；需要追加付费授权才能发任何模型请求。期望父请求一次返回三个工具调用、工具结果后的回复一次，共2次；建议最多4次容纳正常后续。并非保证2次一定完成。仍按实际父/审核/后续/重试全计数，达到上限立即停止，不隐式重置或直连绕过计数器。

新增允许范围仅为上述普通验收文件和公开 HEAD，禁止危险操作、权限破坏、强杀、攻击 fixture 或修改 Codex 核心/二进制。首次完全访问确认标记不重置，历史失败不删除，不为满足 fresh 条件伪造事件。

## 为何仍未宣告完成

治理包 `docs/dev/codex-feature-delivery/QUALITY_GATES.md` D4 明确要求“一次 fresh run 中全部 Must AC PASS”。早前 S4 的 ask 执行（请求18–23）发生在最终 Native 白名单修复之前；最终构建的自动接管、full执行和正常重启证据齐全，ask仅证明设置恢复。这里保留的是最终聚合回归缺口，不再是 AC-003 功能缺口。

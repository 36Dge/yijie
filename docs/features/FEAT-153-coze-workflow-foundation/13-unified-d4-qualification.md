# FEAT-153 统一 D4 验收

2026-09-13，按用户明确授权执行。本轮已完成，最终本地D4通过，见[17](17-d4-final-qualification.md)。11:53键盘修复后的新冻结候选完成dev/packaged独立新建流程与正常重启读回；以下10:41计划及记录保留为阶段历史。前序12中的 packaged 记录只作支持证据，不当成本轮新 run。

## 固定候选与执行范围

本次验证/记录子项 contract-impact=none，不改变跨仓协议、权限、默认值、运行或持久化语义；整体需求仍semantic。发现产品缺陷时先修复、检查、重新冻结候选，再开始受影响的 fresh 验收，不混写候选。

候选记录：[candidate-freeze.json](evidence/step6-d4-20260913-1041/candidate-freeze.json)。12仓HEAD/branch/origin与前序相同；此前记录的源码逐文件摘要未变，五个业务/契约/基础设施候选按canonical算法双采样一致。保留原设计文件、六份Coze原缺失及固定Codex Runtime。无提交、推送、远端变更或对外发布。

统一验收以两个新资源分别标识入口：`FEAT-153 D4 DEV 20260913-1041`、`FEAT-153 D4 APP 20260913-1041`。所有创建、编辑、保存、试运行、内部发布和版本执行必须从真实App发起；脚本只做认证readiness、正常生命周期、定向测试和固定合成资源的API/数据库只读核对。禁止API补写、数据库seed、时间加速、攻击注入、权限破坏、强杀和二进制伪装。

## dev 人工操作 A：完整业务闭环

在本轮由标准 `pnpm tauri:dev` 启动的易界窗口操作。无需登录。请保留窗口，完成后告知结果；出现300秒会话到期时，用页面“重新连接”继续。

1. 打开“工作流”，创建名称为 `FEAT-153 D4 DEV 20260913-1041` 的流程。确认是同一App内画布，开始和结束首次完整入画。
2. 先点击“试运行草稿”：此时图未完成，应明确提示需要检查节点连线。只尝试一次，不把普通错误当作执行成功。
3. 添加文本节点，前缀填 `DEV D4 一：`。从开始右圆点连到文本左圆点，再从文本右圆点连到结束左圆点；拖动文本节点到易辨认的新位置。先离开输入框再拖动节点/连线。保存草稿。
4. 返回列表，重开同名流程，核对前缀、两条连线、三个节点和刚才的位置仍在，首次自动适应画布正常。
5. 试运行输入填 `验收文本`，点击试运行。若显示运行中，使用“更新试运行结果”；终态应成功、输出 `DEV D4 一：验收文本`，三节点都有结果。随后内部发布v0.0.1。
6. 将前缀改为 `DEV D4 二：` 并保存，确认此时内部发布禁用，提示先成功试运行当前草稿。再试运行成功，内部发布v0.0.2。
7. 在下方“执行内部版本”明确填版本 `v0.0.1`，运行输入填 `固定版验收`，执行一次。若状态未终结，点“更新结果”。结果应仍为 `DEV D4 一：固定版验收`；点“刷新历史”核对对应记录，展开“节点结果”核对三个节点。历史与结果是两个显式查询快照。
8. 完成后保留App，回复“A完成”及任何不符现象。无需手抄长ID；由只读证据核对实际资源、revision、run和receipt。不要把未执行的步骤报告为通过。

## dev 人工操作 B：草稿、会话和键盘边界

A经双方核对后再开始B。把前缀改为 `DEV D4 到期草稿：`，保持未保存，等待真实300秒会话自然到期；页面应保留画布并禁写。显式重新连接后原草稿仍在，再保存。使用Tab/Shift+Tab/Enter验证新增输入和按钮可到达、焦点可见；未保存离开时“继续编辑”可取消并保留草稿、正确标题和原路由。

首次打开中取消：在正常返回列表后，下一次打开如能看到“取消打开”，执行一次并检查回到列表、没有迟到重开，随后可重新打开。若按钮因正常响应很快而未捕获，报告“未命中”，不通过延迟注入或反复破坏环境制造窗口；正常异步顺序的UI/native定向测试另记，不能冒称真实点击成功。

## 验收记录与判定

| 检查 | 本轮状态 | 证据原则 |
| --- | --- | --- |
| 候选/来源/已有工作区 | PASS | 当前双采样及既有文件摘要 |
| dev A完整主链路/普通错误修正 | PASS，最终见17 | 用户实际操作反馈 + 当前API/两库/原生日志 |
| dev B自然到期/重连/关闭/键盘 | PASS，最终见17 | 用户实际反馈 + 原生关闭确认/保存读回 |
| 首次打开中取消 | 真实未命中；普通时序测试PASS | 真实命中与正常定向测试分别记录 |
| 新增可访问性/主题尺寸 | 核心可用PASS，完整axe未执行 | 新增控件检查与实际App观察，不继承全页axe |
| packaged fresh主链路/边界/正常重开 | PASS，最终见17 | 同候选独立新资源，实际App与只读证据 |
| 最终完整性/正常清理/跨仓差异 | PASS，最终见17 | 最终摘要复核、正常退出、保留卷与提交顺序 |

已有白底青柠说明1.28:1的配色例外按用户要求保留，新增控件单独核验。模型、商家、平台、付费调用预算均0。完整Must AC、真实happy path和代表性failure/retry未全部通过前，feature.yaml和D4保持未完成。


## 本轮已完成的独立核对

2026-09-13 10:41候选冻结，五候选双采样及检查后摘要一致。canonical up后epoch `97c08010-8e97-45e0-b044-67d793281b91`，六服务healthy、两次migration exit0、宿主认证ready。标准dev已实际启动PID4642，原生初始1180×780/scale2；当前有意保留App与栈等待人工操作，不将有意保留写成清理失败或已退出。

16项界面/状态、13项native、11项Contracts、13项Infra静态测试，以及API普通race测试和Coze正常测试通过；当前生成检查后源码摘要不变。首次打开取消的普通异步顺序已有UI/native测试支持，真实App是否命中仍分开记录。五个实际public HTTP资源与dev binary中机器凭据匹配0，不代表检查了浏览器heap或MessagePort。见[工程检查](evidence/step6-d4-20260913-1041/engineering-checks.json)。

新增可访问性源码已核对字段label/aria-label、状态/错误提示、iframe标题、到期inert及2px焦点样式；12组声明的正文/提示/错误/焦点颜色对达到对应参考阈值。该计算没有覆盖disabled透明度、所有边框/端口或浏览器实际样式。画布节点拖动与端口连线的local adapter目前依赖鼠标，没有实现local纯键盘替代；真实键盘可达性及图操作范围须单独核验，不能据此宣称完整WCAG/全键盘通过。见[源码可访问性核对](evidence/step6-d4-20260913-1041/accessibility-source-review.json)。

六仓差异和后续契约优先的提交/激活顺序已整理在[14](14-cross-repository-delivery-order.md)，没有执行任何提交或推送。当前缺口为人工dev A/B和后续当前候选的packaged fresh流程；D4保持NOT RUN。


## 11:08 人工 A 完成反馈与证据对应

用户明确回复“A完成”，该人工反馈已保存在[manual-A-user-feedback.json](evidence/step6-d4-20260913-1041/manual-A-user-feedback.json)。随后对当前epoch的全部9条合成流程与各自全部运行历史进行了有界GET核对：无未读分页，未找到本轮约定名称，自本轮候选冻结以来没有新增run；列表revision/更新时间仍为前序记录。当前dev PID4642保持运行，也没有新的native关闭会话输出。

这些读回事实尚不能与A的人工反馈对应，已请求实际操作的流程名称、页面版本或输出以定位环境/资源；不否定用户反馈、不通过API补造流程，也不将不对应的数据标为PASS。B及统一D4的后续依赖步骤等待对应关系确认。详见[A-correlation-readonly.json](evidence/step6-d4-20260913-1041/A-correlation-readonly.json)。


## 11:50 AX/CGEvent 接手 dev A/B

用户明确授权原生AX/CGEvent后，已在标准dev PID4642独立新建本轮约定流程`7684858221588119552`。创建、普通invalid_draft、节点/连线/位置编辑、保存返回重开、两次试运行/发布及显式旧v0.0.1执行均通过真实UI完成。三次run成功，API与两库固定scope、3节点/2连线及共享回执一致。该独立证据替代当前dev主链路缺口，不改写此前人工A反馈未匹配的历史事实。

真实300秒会话自然到期后，画布保留未保存的“DEV D4 到期草稿：”，AX不再暴露inert编辑控件；Enter显式重新连接后保存为`7684861162336288768`。Tab/ShiftTab/Enter可操作已有字段/按钮；未保存切Chat后，Tab到“继续编辑”并Enter取消，草稿和“工作流”标题保留；随后正常放弃仅本次未保存合成改动并进入Chat，重开读回已保存到期稿。native四代会话均返回remote_closed=true。App通过正常Cmd+Q退出，launcher exit0，自有相关PID均消失，无信号/强杀。

当前dev A/B行为证据通过，但发现画布移动/连接只有鼠标入口，AC-010尚不完整。将补上等效键盘控件，再冻结新候选，重新完成受影响的dev及packaged fresh验收；不能把当前分项通过提升为D4。首次打开取消在自然快速响应中未命中，普通pending-open定向测试另记PASS。实际AX凭据扫描0不代表heap/MessagePort检查。详见[dev证据汇总](evidence/step6-d4-20260913-1041/dev-qualified-before-keyboard-fix.json)。

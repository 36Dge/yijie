# FEAT-153 画布键盘等效操作补齐

2026-09-13，统一D4实际验收发现AC-010缺口后正常修复。本子项contract-impact=none：仅在Coze本地编辑器为原有图移动、两条允许连线的增加/删除提供键盘入口；仍由同一WorkflowDocument和现有保存路径产生既有canvas格式。不改变跨仓/进程wire、权限、scope、会话、持久化格式、版本/执行解释或部署接口；整体FEAT-153仍semantic。

最小改动：节点标题可Tab聚焦，方向键移动10画布单位，Shift+方向键移动50；可见焦点与简短使用提示。开始与文本节点各提供一个具名按钮连接/断开到下一合法节点，Tab/Enter/Space可操作；仍仅允许开始→文本→结束，缺少文本时禁用。复用原生Coze/Flowgram节点、line manager和既有语义颜色，无新依赖/原生命令/服务。字段错误补上关联标识。鼠标端口和拖动继续保留。

先正常停止App/栈，保留全部数据卷，再修改本地适配器并使用canonical构建、静态检查和既有普通定向测试。冻结新候选后在dev与packaged新建合成流程，至少一次全键盘编辑/连线/移动/保存读回；复验错误修正、版本执行、到期重连、正常重启以及亮暗/两尺寸。完成前统一D4保持NOT RUN。禁止攻击fixture、故障注入、强杀、权限破坏、二进制替换和API补造业务结果。


2026-09-13 收口：授权后的dev与packaged真实验收、键盘修复复验和正常重启读回均完成，最终本地D4结论见[17](17-d4-final-qualification.md)。保留前述方案/准备阶段的原始状态，不将当时未执行改成历史通过。

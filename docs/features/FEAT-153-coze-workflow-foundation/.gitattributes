# Preserve captured CLI output byte-for-byte, including table padding and final blank lines.
# Source, Markdown, JSON, YAML and helper scripts retain normal whitespace checks.
evidence/**/*.txt -whitespace

# Coze 原生轻量青柠配色调整

FEAT-153 local 产品追加，Owner：段成威。2026-09-14，用户否定23中的重石墨图标底板、线条和额外描边，并明确确认：保留Coze原生细边界、轻阴影及节点标题区淡渐变，以青柠色系替换原蓝紫主题色。此次不重画页面或节点，前述视觉通过不等同用户已认可23审美；本次按新的确认单独实现与验收。状态：实现完成，canonical29真实packaged主题追加验收PASS；最终审美反馈由用户查看实际截图确认，不等同新一轮统一D4或生产资格。

`contract-impact = none`：仅调整嵌入文档的颜色、阴影、视觉轮廓及原组件的可选颜色接口；不改变图形路径、布局结构、命中区域、HTTP/bridge、身份/会话、工作流数据或执行语义，无新增依赖、权限、迁移或公开发布。仍以Desktop活跃palette为品牌来源，用户的最新视觉确认覆盖23将品牌装饰改为石墨的选择。

## 已确认的视觉目标

- 节点标题恢复Coze原来的8%主色到主题卡片底色渐变（浅色为白色），保留方向和覆盖范围。恢复原生两层轻阴影与1px细边框，选中用青柠轮廓。
- 连线、箭头和连接点采用青柠原色及既有active色；连接点外环恢复背景色，保留原生hover动画与点击区域。
- 图标外层透明，去掉Header logo及变量引用图标额外inset轮廓。原始SVG几何不改，开始/结束原底形改青柠，内部白色图形保留；文本纸张和工作流图形为青柠，无石墨底板。
- 实心品牌操作使用青柠+石墨文字且无额外描边；“添加节点”和小型highlight工具恢复浅色底层级。普通输入框用轻中性边，焦点采用主题色；键盘可见焦点独立存在，不变成静态图标/按钮边框。
- 小尺寸主题文字以品牌色与既有文字色派生可读青柠深浅阶，普通正文保持石墨。错误/警告/成功保留各自真实语义，前轮修好的暗色正文和错误可读性继续保留。

## 实施与边界

修改集中在`workflow-local/src/yijie-coze-theme.css`；原生ThemedNodeIcon增加透明surface和独立glyph变量，Header logo和NodeIconOutlined共用可关闭outline变量。默认无local变量时沿用上游fallback，不改SVG资源/工作流icon URL。原生NodeHeader、连线和端口已有颜色接口直接复用，不构建第二套画布。

实施开始时App PID35311存在用户未保存内容，用户明确“不保存这份工作流，直接可以退出”。已通过原生“返回工作流→放弃本地内容并返回→Cmd+Q”正常退出，随后workflow-stop确认STOPPED，卷保留；本轮没有保存该草稿。`evidence/coze-lime-refinement-20260914/workspace-before.json`与`theme-before.css`记录CSS调整基线及现有工作树；历史混合改动和六份Coze文档删除保持。

## 本轮验证

1. 主题projection check、source lock、diff检查及标准build/check；只复核相对26的真实增量，保留现有CSP、资产预算和依赖锁。
2. 实际packaged核对浅色渐变、普通/选中节点、图标无底板/描边、青柠连线与圆点、实心/浅底按钮、原生侧栏和输入；补深色、两种实际窗口尺寸及必要键盘焦点观察。
3. 用独立合成流程完成创建、编排、保存重开及一次本地文本试运行。既有用户流程不编辑、不删除；不扩大到商家/模型/外部调用。
4. 保留当前源码候选和实际截图/结果；原统一D4、全量dev、整页可访问性和未接入Coze业务不继承为本次通过。禁止强杀、攻击fixture或权限破坏测试。


## 已完成的源码检查

原生三文件有限检查（1 TS语法、2 LESS编译）、source preservation 62 overlays及diff检查通过。独立实现后复核确认default/hover/active与disabled顺序正确，渐变沿用原8%公式，icon透明surface、原白色glyph及outline=none生效接口闭合；错误/禁用语义保留。本地小文字accent-ink由品牌35%与正文色派生，源计算浅色白底对比度5.53、暗色card底12.89；这些只作为颜色角色校核，不冒称浏览器实测或整页WCAG。

27标准build配对类型无新增诊断，保留126项原上游诊断。26→27真实资产379份不变，变化2 CSS、3 JS及HTML；逐文件证明native9484仅原NodeIconOutlined.boxShadow模板变化，worker7605仅fullhash，main仅fullhash和3个资产引用。原执行位点不增加，原45份CSP目录只登记3个精确新摘要，42份不变；3项目录检查通过。28按最终源正常构建/check后再激活。

## 28 真实 App 观察与回归

28标准build/check通过，27/28全部385资产逐字节一致，manifest `6cf9228ae89480f94197ff48193fdaa0e0580a65cae893ae8c93001489b12def`。标准packaged App PID43826实际显示：原生8%渐变与轻阴影恢复，节点/引用图标没有额外轮廓，白色原glyph配青柠底形；连接线和点为青柠，无石墨外圈。“添加节点”为浅青柠，“试运行”为无额外边框的实心青柠。原生键盘选中保留独立可见焦点，移开焦点后选中节点仅1px青柠边。暗色输入正文与变量可读，普通拼写错误仍用独立红色，恢复后回到已保存。

新合成流程`7685078769316397056 / FEAT153 青柠轻量验收 0914`实际创建、添加Text、键盘连接Start→Text→End并自动布局；保存`轻盈青柠：{{input}}`后返回重开保持内容。自然会话到期在此过程中被观察到，正常返回再打开成功；不把这个观察冒称完整到期重连资格。实际试运行`7685079889996677120`输入`原生渐变`，输出`轻盈青柠：原生渐变`。`final28.json`和`final28-verification.json`共13项只读核对通过，API/MySQL图与运行一致，两库归属/回执一致；本轮没有内部发布，不能借用23的版本执行证据。

两个既有流程以本轮新建`before-ui-baseline28.json`为基线，其after-ui28完整双投影响应保持一致；没有拿历史时点假定用户期间未改。

真实UI发现顶部工作流头像无src时，原CozAvatar默认添加实心品牌底和边框；透明内层使青柠glyph与同色背景重合。已正常保存新合成流程、返回、Cmd+Q退出，并STOPPED后只追加一条CSS：仅匹配包含原workflow SVG的platform avatar，背景及边框颜色透明，保留原尺寸与SVG。这是29的唯一增量，不改JS或业务逻辑，需实际复验顶部图形恢复。

## 29 最终代码候选与锁屏状态

29标准build/check通过，manifest `2a2fa3d5ac83d50994970b8ba62077f7743366aab8b683da171761e06e0b2a44`，source_digest `8ccbc99c0529a85dc93e9694d53b1b51e9c441bd54af52c5fc19cd4ea052a689`。235份JavaScript与28逐字节一致；385份资产中383份一致，仅entry CSS新增具名workflow avatar的透明背景/边框规则，HTML更新CSS引用。原CSP目录、预算、依赖锁无改动，未重复30。

标准register/build/up及激活核对通过，所有385份资产与manifest一致、实际入口相同，公开资源中当前两种机器凭据匹配为0。标准packaged App PID47855已启动。在请求读取真实窗口时，CUA明确报告Mac已锁定且无法自动解锁，尚未取得29窗口/头像截图；已请求用户手动解锁，保留App/服务并停止UI操作。不得把28的已有截图当作头像修正后的29通过，也不得绕过锁屏或伪造两种窗口尺寸结果。

## 29 解锁后的最终验收

用户回复“已解锁”后继续在同一个真实packaged App观察。顶部工作流图形在浅色和深色下均恢复可见，外层背景透明；节点标题和原生侧栏标题保持淡青柠渐变，原图标几何及白色glyph不变。青柠连接线、箭头、连接点没有石墨描边，图标和试运行按钮没有额外边框；选中节点保持细青柠轮廓，未选中恢复轻中性边界。原生侧栏正文、变量引用与输入内容清晰可读。

实际窗口尺寸通过CGWindow观察为1180×760及1346×848点，两种主题均观察并截图。较大窗口请求1440×850但被macOS约束，记录实际尺寸，不把请求值写成通过值。仅用原生“自适应”调整最终视野，不通过再次自动布局或保存改写坐标。最终恢复浅色，App保留在已保存的完整画布，保存按钮禁用；没有内部发布或版本执行操作。

| 实际证据 | 范围 |
|---|---|
| [浅色完整画布](evidence/coze-lime-refinement-20260914/final29-light-large-canvas.png) | 最终头像、淡渐变、青柠线点、普通节点、工具栏 |
| [浅色小窗口侧栏](evidence/coze-lime-refinement-20260914/final29-light-small-panel.png) / [浅色大窗口侧栏](evidence/coze-lime-refinement-20260914/final29-light-large-panel.png) | 1180×760 / 1346×848，原生文本节点面板与选中效果 |
| [深色小窗口侧栏](evidence/coze-lime-refinement-20260914/final29-dark-small-panel.png) / [深色大窗口侧栏](evidence/coze-lime-refinement-20260914/final29-dark-large-panel.png) | 两尺寸下头像、渐变、图标和正文 |
| [当前草稿试运行](evidence/coze-lime-refinement-20260914/final29-dark-trial.png) | 真实succeeded及输出，未点击内部发布 |
| `ui29-observations.json` / `final29-*-ax.json` | 实际尺寸、截图摘要、已保存/结果状态；保存的AX文本中当前两类机器凭据匹配为0，非堆内存或全传输审计 |

### 持久化对比的真实范围

`final28-29-comparison.json`记录了快照之间的一次真实completed save：本地02:04:25、operation `01a09bf1-1e52-74f2-b0ce-307e600d31bf`，revision从`7685079325527244800`变为`7685082330511179776`。三个节点坐标及Start的`trigger_parameters`数组发生变化，不能写成“重启前后完整业务数据相等”；快照本身也不足以归因具体操作人。两个既有流程与本轮before-ui28完整双投影相同，旧运行、历史、执行和三个既有操作回执全部保留，新save在两库一致。

离线源码核对确认：原生Start的`transformOnSubmit`会根据已配置触发参数重算`trigger_parameters`，未配置时为空；该段与锁定上游相同，本轮未修改。当前受限执行仍读取Start.outputs及实际input，不消费这个显示字段。去掉坐标和该字段后，两份图严格相等。为避免用旧revision的试运行替代新草稿证据，29又通过真实App执行当前已保存草稿：输入`最终青柠复验`，实际输出`轻盈青柠：最终青柠复验`，页面终态succeeded，已保存状态保持。

该次运行`7685083794788843520`绑定最新草稿revision `7685082330511179776`。后续[13项定向只读核对](evidence/coze-lime-refinement-20260914/final29-after-trial.json)全部通过：API/MySQL均为succeeded且输入输出相同，两库completed回执一致，画布、名称、描述和revision未因试运行改变，既有历史及旧两个流程完整投影保留。工作流响应的`updated_at_ms`因真实试运行正常更新；不声称该流程整份响应或全数据库完全相等。所有核对连接和锁均已释放。

最终源码/资源保持29冻结候选，没有再次构建；六仓HEAD、分支、远端与本轮基线相同，Desktop颜色源摘要不变。本次未Git提交/推送。原dev、统一D4、整页可访问性、未接入节点与生产资格不重跑或扩充；用户禁止的破坏性/攻击测试均未执行。

## 暂停后恢复收尾

用户曾要求暂停，交接记录见[25](25-lime-refinement-pause-handoff.md)。随后明确“继续执行”，本次恢复仅补齐最后的运行证据、交付索引及文档检查。恢复时读取进程表确认原PID47855及其他`yijie-desktop`进程均不存在，故不沿用旧AX状态或声称App仍在展示；本次没有重启App、操作画布或更改服务。上节截图与真实操作结论保留其原验收时点。

收尾检查通过：[检查原始结果](evidence/coze-lime-refinement-20260914/resume-closeout-checks.json)。`pnpm lint`验证11仓清单和治理规则，`pnpm test`通过50项元仓治理/文档数据验证，7份元仓Shell逐文件语法检查通过。现有编辑器`check`再次核对源码摘要、资源哈希和契约锁，保持29候选，不触发重建或UI运行。离线复核7份实际PNG摘要及尺寸、28/29保留产物和最后13项业务观察一致。上述元仓测试不代替真实App或工作流业务测试。

# FEAT-154 需求落盘记录

当前执行收口见[23](23-final-d4-qualification.md)，统一状态见[16](16-delivery-status-and-evidence.md)。以下按轮次保留历史，不将早期未实施/NOT RUN视为当前状态。

## 1. 初始授权（历史）

2026-09-15：仅依据PDF及三个Markdown起草FEAT-154，暂不执行实现。状态draft，D0 pending，D4 NOT RUN。前序FEAT-153批准/预算/提交许可不自动转用。

## 2. 首轮实际完成的文档工作（历史）

| 项目 | 结果 | 范围 |
|---|---|---|
| PDF读取 | 8页文字提取及全部渲染图片人工视觉读取 | 页面1–3任务与限制；4–8控件/面板参考 |
| 节点资料 | 30节点完整分工阅读，68小节摘录 | 299输入+64配置+271输出=634顶层字段行 |
| 源快照 | 4文件逐字节保留并记录SHA-256 | 只作资料，不执行其中业务语句 |
| 工程调查 | 确认真实WorkflowPlayground/NodePanel及多层三节点限制 | 主入口、变更/保存、重连、dirty保护；只读 |
| 需求起草 | 00/03/04/05、feature.yaml及交付/验证记录 | 10项Must、状态、UI取舍、默认值缺口与来源 |
| 独立审查 | 子任务只读复核字段及设计状态边界 | 补入catalog/instance区分、原生副作用、重连与宿主离开保护；不是Owner批准 |

本次唯一仓库写入路径为本需求目录，不修改业务代码、源资料原件或既有FEAT-153内容。文档检查实际命令与结果见[02](02-verification.md)。

## 3. 后续候选顺序（未执行）

先确认需求/页面内存生命周期与既有UI通知契约语义 → 原生目录与展示元数据 → 卡片/属性控件与renderer状态保护 → 同源聚焦检查 → 同一App真实UI验收。这是技术依赖顺序，不建立生产治理切片，也不构成启动实施的指令。

全部30节点仍在范围。时间盒为未来工作提醒，不允许自动删节点或启用业务接口。具体内容见05。

## 4. 操作与费用

| 类别 | 本轮许可 | 已用 |
|---|---|---:|
| 模型/平台/付费调用 | 无 | 0 |
| 商家/生产写入或消息发送 | 无 | 0 |
| 破坏性操作/攻击测试 | 用户永久禁止 | 0 |
| App/服务启动停止、业务构建 | 本轮不实施 | 0 |
| Git提交/推送/分支/部署 | 本轮不实施 | 0 |

## 5. 未决项与限制

UI字段内层未定义项保持引用或未选择；推荐仅页面内存配置，不持久化。未来dirty/离开保护需核对权威语义，保守semantic不代表通过兼容验证。无产品结果/业务运行/D4证据。

## 6. 后续执行计划请求

2026-09-15，用户询问下一步与执行计划；已新增[06执行计划](06-execution-plan.md)，推荐先定稿组件接入/草稿保护和01/10/17交互，再在获得实施授权后连续完成全量30节点。复核12个关键源码摘要无变化。仅规划，不把此请求视为实施或D0批准；没有业务源码、构建、服务或Git操作。

## 7. 执行第1步：方案定稿

2026-09-15，用户明确要求执行第1步。完成注册/类型/实例、字段/控件和生命周期/契约的三路只读调查，形成07–10定稿附件及3代表节点交互示意。用户已明确选择“保持纯前端范围：页面离开确认，关闭应用不保证保留”。该决定已同步相关需求，不新增native退出保护。当前D0完整方案确认及机器检查结果以07/02为准；不执行第2步、跨仓生成、业务构建或App/服务启停。

## 8. D0用户确认与第1步收尾

用户在本轮明确回复“确认这份方案”。已对照review-candidate逐项验证03/04/07/08/09/10及交互示意SHA-256相等，记录实际Owner确认。仅更新需求状态并运行D0门禁；结果见02第7节。全部产品Must保持pending，第2步实现、契约生成/同步、App/服务启停、Git提交推送及D4仍未执行。

## 2026-09-15 · 第2步实施授权

用户在确认D0方案后明确要求：“第二步方案是什么？输出后，开始执行”。已说明实施顺序并开始共用框架及01/10/17三个样板；原始工作区、SHA和保护文件摘要记录于 `evidence/step2-implementation/workspace-before.json`。后续实际结果以新增实施记录为准，D4与30节点完整验收保持NOT RUN。

## 2026-09-15 · 第2步代码与离线检查收口

已完成source-first1.4候选、三消费者同步、原生共用骨架及01/10/17样板。47项Coze聚焦、16项Contracts、23项Desktop与50项元仓测试通过；固定上游类型基线126条不变，新增0。独立复核后修复远端更新队列、bootstrap与原生确认时序、加载期提交守卫、同tick添加快照及清空必填触达状态。

发现现有Coze/MySQL容器保留前一日退出137，正常stop门禁拒绝；已停止后续激活，保留数据。第2步实际App验收尚未完成，D4保持NOT RUN，不自动进入其余27节点实施。详见[11](11-step2-implementation.md)与evidence/step2-implementation。

## 2026-09-15 · 下一步执行方案

用户询问下一步优先事项与执行方案。本轮只读复核现有容器与源码：原Coze/MySQL退出137保持、所有登记容器未运行、旧manifest保持、56个Coze改动文件无漂移。新增[12恢复与第2步验收计划](12-recovery-and-step2-verification-plan.md)，推荐显式恢复入口→同数据集正常恢复/退出→新候选激活→三个样板真实验收。未实施恢复、未启停或改写当前交付状态；D4仍NOT RUN。

## 恢复执行中的范围重申

用户再次明确整个FEAT-154只注册30个电商节点并实现画布相关展示，不实现节点接口调用和节点业务处理。已同步00与feature.yaml；继续执行已批准的环境恢复及纯UI验收，全部新节点保持无执行/业务副作用。

## 五步恢复与样板验收执行授权

用户明确要求执行12中的全部五步。恢复实现的部署语义影响为semantic，权威在Infra与API/Coze私有存储；不新增节点接口或业务逻辑。已保存七仓工作区与8容器原状态，生成恢复ID `f910d233-e4fc-41bd-b02d-783897bd5339`；8项恢复聚焦测试和13项原生命周期静态检查通过。执行中继续记录实际副本、恢复、激活及UI结果，未完成项不登记PASS。

## 用户追加确认：保留试运行交互

用户明确说明“不实现接口逻辑”不代表禁用相关界面。FEAT-154的试运行入口保持可点击，点击仅通过前端提示“暂未接入试运行”，不发接口请求、不执行节点业务、不生成模拟运行结果。此确认更新此前方案中“禁用试运行按钮”的表述；实际提交保护和整个30节点的纯UI边界保持。

## 恢复完成与R6交互复验

原环境已受控恢复并正常退出，新候选已按canonical流程激活。R5实际验证57字段、纯前端试运行提示、独立实例、自然重连与原稿保护；持久业务事实保持。发现菜单删除确认、删除后的引用刷新与节点焦点快捷键问题后，R6补齐局部前端处理并通过构建、原调用点CSP审查和聚焦检查。当前R6已运行，实际复验中，未标记第2步或D4完成。

目录拖入的自动操作未成功；用户回复“可以，拖入成功后告诉你”，当前等待其实际操作结果，此回复不是拖入已通过的证据。详见13及recovery-execution目录。

## R7核心验收后、暗色决定前（历史）

R7核心交互与持久数据比对通过，15个保护文件摘要保持、七仓diff空白检查通过，无提交推送。原稿及本次测试名称已清理到“已保存”，App与本地服务仍运行。三个样板的暗色实测需临时修改系统外观，已请求单独确认且尚未执行；第2步整体未标完成，全部30节点及D4未完成。

## 2026-09-16 · 第2步完成，暗色实测延期（历史阶段）

用户明确回复“保持系统外观，暗色实测留到后续”。保持现有系统外观，将暗色真实App验收登记为NOT RUN、用户决定延期；第2步共用骨架及01/10/17三样板核心验收据此完成。其余27节点、完整主题/尺寸矩阵与全需求D4仍待实施/验收，feature和implementation保持in_progress。

本轮只同步文档与证据，契约影响none，无运行时或系统设置操作。此前代码/恢复的semantic影响及R5/R6/R7历史证据保持。整个FEAT-154无节点接口调用与业务处理，试运行保持可点击、仅作前端提示。决定与完成范围见[收口记录](evidence/recovery-execution/dark-theme-deferral-and-step2-closure.json)，真实验收见[13](13-recovery-and-sample-verification.md)。

## 2026-09-16 · 全量30节点实现与R2核心验收

用户已授权补齐27节点的五步方案。完成30目录／634字段、中文嵌套控件、节点专用模式与默认提示、小数和对象默认值；本轮增量contract-impact=none。通过现有显式入口恢复原环境并正常停止后，canonical构建、登记并激活R2；R1发现的嵌套主标签问题已修正，R2完成同一候选全量代表字段复验。

43项聚焦、3项CSP检查、构建及元仓50项检查通过。用户回复05目录拖入“成功”；直接核对新增05_1及独立填写，核心交互全部通过。试运行按钮只显示前端提示，存储事实无业务变化。原三节点和原名称恢复到“已保存”，App与服务保持运行；没有提交或推送。

源码实现完成；AC-009尚缺1440×900及用户延期的暗色/完整键盘尺寸矩阵，完整D4仍NOT RUN。实际结果与限制见[14](14-full-node-ui-implementation.md)。

## 2026-09-16 · AC-009尺寸/键盘补验与三处修复

用户授权执行五步验收。保持系统外观，正常启动已有Docker/原数据服务与canonical App；原退出均0，无异常恢复。实际发现并修复目录节点焦点被裁切、Esc无法从节点退出、Tooltip与节点重复Tab停靠。每次构建前正常退出App及停止服务，原稿和数据保持。

最终候选30次Tab对应30个目录按钮，浅色1180×760下的完整本轮键盘、焦点、复杂表单、长标题及恢复检查通过；43聚焦/3CSP/标准构建通过，新增类型问题0。节点和面板试运行仍只提示，无接口业务写入。原稿恢复到已保存，窗口还原1180×780，App/服务留运行，无Git提交推送。

1440×900未达到，1512×875不替代；暗色仍延期，AC-009及D4保持未完成。当前证据见[15](15-ac009-keyboard-and-window-verification.md)。

## 2026-09-16 · 用户不允许调整系统显示缩放

大窗口预检已确认当前候选/源码/契约锁一致、六项服务健康，原稿显示“已保存”；系统仍为1512×982默认显示空间，1800×1169选项可用。请求单独确认后，用户明确回复“不允许”。未修改系统显示或主题设置，停止依赖该调整的1440×900验收，不重复请求同一调整。

保留30节点实现及1180×760浅色键盘验收成果；1440×900为NOT RUN，暗色继续延期。此决定仅拒绝系统设置变更，不等于排除AC-009或批准D4，二者保持未完成。来源：[用户决定](evidence/light-large-verification/owner-display-change-decision.json)。

## 2026-09-16 · 整理交付证据与跨仓提交清单

用户明确要求执行证据整理、跨仓归属、交付状态固定及待授权提交清单四项。本轮`contract-impact=none`，只写FEAT-154元仓资料；保留此前semantic历史。选取33份必要日志完整脱敏归档，记录原文/归档双摘要及178处本地路径替换，其余38份原日志保留本地。真实App截图本地导出0张，缺口及审阅影响逐轮登记，未用3张静态D0图冒充。

新增16统一入口及17逐仓提交计划；展开Contracts10、Coze72（含独立主题1）、Desktop7、API4、Infra6的具体文件，并单列元仓主题14文件。源快照、Contracts源和消费者、恢复依赖及最后元仓证据的顺序明确，真实Git动作仍待单独授权。实现complete、AC-009 pending、D4 NOT RUN保持；大窗口设置拒绝与暗色延期保持。实际检查见[evidence/delivery-package/package-checks.json](evidence/delivery-package/package-checks.json)。

## 2026-09-16 · 授权后的逐仓本地提交

用户明确要求执行“单独主题依赖→Contracts源→三个消费者固定和检查→实现/Infra/元仓提交→统一入口”的方案，并在中断后要求继续。7个依赖/源码提交已形成，元仓完整包作为第8个提交；实际完整SHA见[18](18-local-commit-baseline.md)及[机器基线](evidence/local-commits/commit-baseline.json)。无推送、tag、发布、分支切换或远端更改。

canonical同步仅改变API/Coze/Desktop三份consumer lock，固定Contracts实际提交。Coze首次staged diff检查发现原未跟踪reference-picker.tsx多一个末尾LF，移除后通过；失败和修正记录均保留。源资料1-8.md及5份归档日志的原文空格保持，不伪报零whitespace警告。正常聚焦检查通过，现存App/编辑器字节未变；未新构建、登记、启动或真实UI验收。实现完成、AC-009 pending、D4 NOT RUN保持。

## 2026-09-16 · 已提交基线构建、激活和定向回归

用户授权执行五步方案。旧App通过Cmd+Q退出，Infra正常停止，全部记录容器exit0；App数值退出码不可取得，未推定。随后Coze canonical构建、登记、服务build/up及Desktop packaged入口完成；新manifest `b90096c02c9dd6b332344eafc1e817fa717db7f764e245bcc70c19df6ae7a06c`、epoch `84966cf7-bf7d-479d-a1e5-5abf81717be7`。源码仓保持已提交HEAD且干净，15个主题文件不变，无新增业务实现。

当前浅色1180×760完成30目录、05/15代表表单、A/B实例隔离、自然重连、删除撤销、试运行提示、取消离开及原稿恢复。试运行前后所有私有事实相等，UI整轮业务事实不变。截图导出尝试未产生本地文件，缺口如实登记。原稿“已保存”、导航和窗口恢复，新App/服务保留运行。详见19；AC-009 pending、D4 NOT RUN，未调整显示/外观、未提交推送。

## D4启动、搜索修复与锁屏暂停（历史，2026-09-16）

用户追加要求继续D4。19五步收口后开启新序列，01–08代表字段检查通过，09完整名称搜索复现失败；本地匹配空格处理已修复，45聚焦检查/3 CSP检查、标准构建登记及新App启动通过。当前manifest为77878acf…，含4个未提交Coze修复/测试/审阅文件。新候选UI复验因Mac锁定暂停，已请求手动解锁；当前不能声明搜索修复已通过真实UI或D4通过。具体边界、证据和恢复点见[20](20-d4-current-environment.md)。

## 2026-09-16 · 当前环境共用复验与提交收口

上节锁屏暂停属于历史；用户解锁并继续授权剩余工作后，同一77878acf候选的30节点、搜索、双实例、普通格式错误、删除取消、引用失效/撤销/重做、历史、试运行无调用、可选布尔/键盘、发布模式、离开取消和恢复原稿完成。原名称及三节点两连线回到“已保存”，窗口1180×780恢复。全轮非审计事实相等，试运行窄窗口全部事实/审计相等。Coze修复已提交6d5b309519c2c027fee4a37a107f15c56d554645；元仓证据随本节归档，无推送。1440×900与暗色继续NOT RUN，截图本地导出0张，AC-009 pending、完整D4未通过。具体结果和工具限制见[20](20-d4-current-environment.md)。

## 2026-09-16 · 已提交来源对齐与真实截图归档

用户授权对齐6d5b3095编辑器并继续执行。标准显式恢复已独立核实正常退出0，历史137保留；canonical重建、检查、登记、原镜像复用启动和Desktop标准增量构建完成。当前manifest b17f052d…、epoch 4b160abe-3fee-459e-971b-78d39421df0d；385项资产字节与20候选一致，仅manifest来源提交对齐。

本轮浅色1180×760目录、完整名称搜索、05零值/对象/焦点、正常重连、两处试运行提示、离开取消及恢复原稿定向回归通过。全部非审计事实不变；试运行全部事实含审计相等。原稿已保存、导航和1180×780恢复。真实截图原字节归档2张，历史0张缺口保持。窗口正常zoom仅达1512×875；未改系统显示或主题，1440×900与暗色仍NOT RUN，AC-009 pending、D4 BLOCKED。本轮无源码修改、无新提交或推送；新增文档/证据尚未提交。当前候选、截图、检查与剩余条件见[21](21-editor-source-alignment.md)。

## 2026-09-17 · 暗色最小窗口验收与媒体图标修复

用户已要求开始暗色验收，临时切换系统深色后发现17–22媒体图标低对比问题；用既有本地图标组件和主题变量修复，原SVG/图标URL保持。正常停止后完成精确CSP审阅、canonical重建/登记/build/up及Desktop启动；当前de585dcc候选含2个未提交Coze文件，基础HEAD6d5b3095，实际source digest与完整身份见[22](22-dark-appearance-verification.md)。

同一候选的暗色1180×760目录30项键盘、17媒体/多行/下拉/侧栏保留、05零值/对象/输出/重连、24长名称、历史与离开弹层以及试运行提示通过；浅色受影响图标回归通过。原稿、导航、1180×780窗口和原浅色均已恢复，业务事实保持，试运行所有事实含审计相等。本轮7张真实截图归档，含修复前1张、修复后暗色5张、浅色1张；加21共9张。

暗色1180×760本轮范围PASS；浅/暗色1440×900仍NOT RUN（默认显示不改，正常zoom实测1512×875），AC-009 pending、D4 BLOCKED。暗色已经实际验收，不再作为整体“用户延期未执行”；剩余为大窗口两种外观及最终完整Must fresh run。本轮修复及元仓证据未提交，21已有未提交成果保持，无推送。

## 2026-09-17 · 同一候选最终D4

当前FEAT-154已完成实现与最终本地真实验收：30节点／634顶层字段，AC-001–010通过，feature=usable、verification=PASS，D4证据见[23](23-final-d4-qualification.md)。当前manifest586e23d3，来源绑定含未提交独立UI调整的实际工作树；同一候选完成全30节点、共用交互及浅/暗色1180×760和1440×900。12张新真实截图归档；原稿、默认显示、浅色及1180×780恢复。本轮改动和记录未提交，无推送。四项独立节点面板调整不计入本需求。

用户新授权显示缩放后实际取得1440×900；正常会话自然到期、重连、引用失效及格式修正均按非破坏方式验证。试运行事实含审计相等，整体非审计事实相等。额外原生菜单路径仍按用户决定跳过，使用正常键盘和页面取消覆盖既定交互。

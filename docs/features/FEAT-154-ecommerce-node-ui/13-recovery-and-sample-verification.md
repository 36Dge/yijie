# FEAT-154 恢复执行与三样板实际验收

## 当前结论

用户已授权12中的五步方案。恢复预检、显式恢复入口、旧环境恢复及正常停止、新候选激活均已完成；三个样板的核心交互实际通过，R7证据显示原数据保留、服务就绪。用户最新明确回复“保持系统外观，暗色实测留到后续”。**第2步按此范围完成，暗色真实App验收登记为NOT RUN、用户决定延期。**其余27节点和全需求D4继续待实施/验收。

本次仅同步文档与[用户决定记录](evidence/recovery-execution/dark-theme-deferral-and-step2-closure.json)，未修改系统外观或操作App/服务；上述运行时结论引用既有R7证据。

整个FEAT-154只注册30个电商节点并展示画布、字段和页面配置；不实现接口调用或节点业务处理。用户追加明确：试运行入口保持可点击，点击只提示暂未接入，不发接口、不产生模拟结果。此前文档中“禁用试运行按钮”的设计已由此确认更新；实际执行资格仍与页面展示入口分开。

## 当前验证汇总

| 项目 | 真实结论 |
|---|---|
| R7 Coze canonical构建与产物检查 | PASS；TypeScript固定上游126条诊断保留，新增0；385资源，manifest见R7激活记录 |
| Coze源保持 | 15221文件保持、6项原删除保留、78项登记覆盖；新增覆盖仅私有节点历史恢复适配 |
| R7聚焦测试 | 35项通过（含新增历史恢复回归）；值状态5项与CSP3项检查通过，测试范围见原始日志 |
| 恢复脚本 | 9项正常内存测试通过；既有workflow-check 13项通过；不是故障注入或完整数据库恢复演练 |
| 契约/消费者 | 1.4本地候选；原16项Contracts、23项Desktop结果保留，激活再次核对三消费者来源锁；不声明正式发布或全部历史基线兼容 |
| 数据保护 | R5/R6/R7全部业务事实对比保持；R7试运行前后含审计的全部事实也完全相同 |
| 实际UI | 三样板及57字段实测、同类独立实例、用户拖入、输入/空值、模式、删除与撤销重做、引用失效、重连、取消离开、历史保留和恢复原稿均有分轮证据；R7修复项已复验 |
| 主题/窗口 | 原生亮色1180×780通过；暗色实测NOT RUN，用户决定延期；完整主题/尺寸矩阵仍待验收；Cmd+Z未作通过声明，Ctrl+Z/Ctrl+Shift+Z已实测 |
| 第2步交付 | 共用骨架及01/10/17样板核心验收完成，附用户决定延期的暗色实测项；不代表全量30节点或D4完成 |
| 工作区 | 15项受保护文件摘要保持；七仓diff空白检查通过，分支HEAD不变，无Git提交推送 |

## 1–3. 恢复结果

- 恢复ID：`f910d233-e4fc-41bd-b02d-783897bd5339`。保存原容器身份、镜像、挂载、旧385文件前端产物摘要和原始退出137记录；原退出原因仍未知。
- 四个已停止的数据卷生成只读保护副本，共297,880,823字节；保留原卷。此为保护副本，未声称验证过从副本还原数据库。
- Infra增加显式恢复预检、执行、来源修订入口和阶段日志，正常启停门禁保持。API与Coze仅提供各自私有只读存储核验，不增加公共接口、节点处理或业务重放。
- 原四项依赖正常启动后，只读核对130项API操作、117项Coze操作、36次执行、108项节点执行和17个工作流。两项历史storage qualification unknown按既有逐ID证据解释；未清空操作或active slot。
- 原API启动产生一条正常principal审计，其余原业务事实保持。原六个主服务健康，随后全部正常退出0；原迁移容器未参与恢复重启。
- 恢复中修正Docker Desktop历史挂载前缀识别、私有receipt字段大小写和正常启动审计判定；使用追加来源修订链保存各版本依据，未覆盖原快照。

证据：[恢复结果及副本摘要](evidence/recovery-execution/recovery-result.json)。私有原始记录与副本在Infra ignored的 `environments/local/generated/feat-153/recoveries/<恢复ID>/`。

## 4–5. 激活与实际验收过程

按Coze canonical prepare/install/build/check、Infra workflow-editor/build/up/status、Desktop canonical启动执行。源码及1.4候选消费者锁被重新核对，未把旧HEAD伪装成dirty候选完整来源。

真实App早期轮次发现并修复：

1. 原三节点工作流已有文本节点时，旧添加按钮限制误将整个NodePanel禁用。改为读取独立电商添加资格；R2实际能打开三个样板目录。
2. 新卡片在Form上下文之外调用useWatch，引发原生错误占位。补共享节点Form上下文并保留节点模型生命周期；同步按完整ui值更新折叠分组。
3. 按用户最新确认，工具栏、试运行面板与节点卡片保留试运行交互，使用纯前端提示分支；不进入上游执行组件。

R2已观察到自然会话到期、重新连接保留电商节点，以及明确恢复原草稿后回到“已保存”。这些仅为早期轮次结果，不能替代修复后最终三个样板的完整实际验收。

每次更新dist前均正常退出App和停止服务；所有数据卷保留。增量CSP审查只更新核对过的构建字节与原调用点对应关系，运行时CSP权限保持。

## 未完成项目与限制

- 三样板字段、独立实例、点击/拖入、切换保留、撤销/删除、试运行无调用、取消离开、恢复原稿和自然重连已获得下述分轮真实证据；R7历史恢复修复已复验。暗色实际界面按用户决定延期，状态NOT RUN；目前只能确认亮色1180×780，不声明完整主题/尺寸矩阵通过。
- 其余27节点和整个FEAT-154的10项Must、D4仍待后续实现与验收。
- 强杀、权限故障、攻击fixture、危险归档和伪装二进制测试未执行，用户永久禁止；不声明这些故障资格或全仓测试通过。
- 无节点业务、模型、平台或媒体调用；无Git提交推送、生产或公网操作。

## R5实际操作记录（2026-09-15至16日，后续修复复验中）

- 57个顶层字段已在三个原生侧栏逐组、滚动核对；全部输出为只读定义。两份商品采集实例分别保持独立来源、0值和false；图片对象字段与空列表可填写。
- 卡片和流程试运行入口可点击，只出现未接入提示。直接只读核对API操作/资源、Coze操作/执行/节点执行/工作流，六组数据与UI操作前完全相同。
- Listing组装方式会展开已有组件，填写后可切换完整生成；正常会话到期后重连保留样板、侧栏和listing-sample。
- 取消离开、取消恢复保留设计；确认恢复后回到进入设计前的FEAT154未保存名称与原三节点连接，随后正常退出App并STOPPED，原卷保留。
- R5发现菜单删除未确认、删除来源的引用提示滞后，以及节点焦点下原生快捷键不响应，已准备R6修复。目录拖入未成功，原因待确定，不伪报通过。

详细观察：[R5真实操作](evidence/recovery-execution/ui-r5-observations.json)、[持久数据对比](evidence/recovery-execution/ui-r5-persistence-comparison.json)。截图为当前任务内原生CUA工具的直接输出；没有将示意图或未保存的截图路径写成实际证据。

## R6实际复验与历史恢复修正

用户手动拖入图片节点后回复“已经拖入”，随后的CUA状态和截图直接显示新节点，拖入项获得用户操作与实际画布证据。自动操作之前失败不再作为产品缺陷结论。

菜单删除确认/取消保留与引用失效提示已实际通过。Ctrl+Z在准确聚焦节点后可撤销添加；Cmd+Z在自动操作中未见效果，未声明通过。重做/撤销删除发现原Coze历史适配将isClone=true传入私有工厂，被复制保护误拒绝。R7只将电商私有类型的历史恢复改走保留原ID的正常校验路径；原节点、复制入口与数据边界不变。新增测试直接运行该历史适配器与真实私有工厂逻辑，原生文档/clone/merge依赖为明确的单元替身，不能替代后续真实App复验。

R6再次恢复了进入设计前的未保存名称和原图，正常退出App/服务，持久数据六组对比仍完全相同。具体记录：[R6操作](evidence/recovery-execution/ui-r6-observations.json)。R7最终复验仍待完成，暂不将第2步标记完成。

## R7核心交互结论

历史修复真实通过：删除后Ctrl+Z恢复节点及自定义字段history-r7，Ctrl+Shift+Z可重做，再次Ctrl+Z仍恢复原值。最后一个电商节点删除后保持设计保护。R7还实际核验了三个样板、两份采集实例、自动布局、取消离开、历史modal保留、原稿未保存名称恢复以及仅清理本次测试修改后回到“已保存”。

最终试运行前后立即执行私有只读查询，包含API审计在内的所有事实完全相同，新增审计0。全部UI流程结束后的操作、执行、节点执行、资源和工作流六组业务事实也与启动前相同。

原生自动操作中一次悬停菜单定位偏差删除了临时设计图中的原文本节点；显式恢复已还原原节点及两条连接，未按保存或触发业务写入。键盘撤销以直接验证过的Ctrl+Z/Ctrl+Shift+Z记录，Cmd+Z不作通过声明。

证据：[R7操作](evidence/recovery-execution/ui-r7-observations.json)、[R7数据比对](evidence/recovery-execution/ui-r7-persistence-comparison.json)、[R7激活来源](evidence/recovery-execution/activated-runtime-r7.json)。R7记录时App跟随系统亮色；由于没有独立主题开关，当时请求了临时切换系统外观的单独确认。后续用户决定保持系统外观、暗色实测延期，见本页当前结论；R5/R6/R7原始记录保持原样。
